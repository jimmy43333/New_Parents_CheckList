# 新手爸媽準備清單

情侶共編的待產/育兒準備清單 PWA(Vue + Firebase)。使用者依「準備清單」「待產包」「照顧能力表」三個分類追蹤準備進度。

## Language

**分頁列 (Tab Bar)**:
桌機版下,位於頁首下方的橫向分頁選單,用來切換「準備清單/待產包/照顧能力表」三個分類。與「底部導覽列」是同一個元件(`TabBar.vue`)在不同斷點下的兩種呈現形態。
_Avoid_: 分頁選單、tabs

**底部導覽列 (Bottom Navigation Bar)**:
手機版(≤700px)下,固定貼於螢幕底部的分類切換列,icon 在上、label 在下,取代桌機版分頁列的位置,以符合手機拇指熱區的操作習慣。與「分頁列」共用同一元件與資料,僅在斷點內的定位與排版不同。
_Avoid_: 底部 tab bar、bottom tab

**選單分頁 (Menu Tab)**:
分頁列/底部導覽列裡最右邊的第 4 個項目,固定 key 為 `menu`。與「準備清單/待產包/照顧能力表」三個分頁不同,它不對應 `TAB_ORDER`/`DATA` 裡的任何清單資料(沒有完成度、沒有 `TabPanel`),點選後渲染獨立的 `MenuPanel.vue`。
